overwrite: true
schema: "./src/schema.graphql"
documents: null
generates:
  ./src/autogenerated-resolver-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      enumsAsTypes: true
require:
  - ts-node/register
